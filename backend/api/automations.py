"""
Automations CRUD — Phase 3 (T40-T44).
Stub router registered now so import graph is clean.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/automations", tags=["automations"])

# Full implementation in Phase 3
