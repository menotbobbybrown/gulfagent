"""
Billing + Stripe routes — Phase 4 (T63-T70).
Stub router registered now.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/billing", tags=["billing"])

# Full implementation in Phase 4
